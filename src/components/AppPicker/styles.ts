import { COLORS } from "config/colors";
import { StyleSheet } from "react-native";

export default StyleSheet.create({
  text: {
    flex: 1,
    textAlign: "center",
    fontSize: 18,
  },
  placeHolder: {
    flex: 1,
    textAlign: "center",
    color: COLORS.medium,
  },
});
