import { StyleSheet } from "react-native";

import { COLORS } from "config/colors";

export default StyleSheet.create({
  textInput: {
    width: "70%",
    marginLeft: 10,
    fontSize: 18,
    color: COLORS.dark,
  },
});
