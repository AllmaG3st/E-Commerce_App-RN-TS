import { StyleSheet } from "react-native";

export default StyleSheet.create({
  animation: {
    width: 250,
  },
  container: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
